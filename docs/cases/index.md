# FlexNoC Case Library

This case library records failures that an agent should check before treating a FlexNoC export failure as an owner or tool blocker.

| ID | Symptom | Case |
|----|---------|------|
| 001 | `Element ... is not stable`, `A Node has not been placed on any route`, or `Incompatible settings ... datapathRoute` after sparse connectivity generation | [Sparse connectivity stale routes](001_sparse_connectivity_stale_routes.md) |

